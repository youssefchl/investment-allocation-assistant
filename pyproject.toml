[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "invest-assistant"
version = "0.1.0"
description = "Educational investment allocation assistant (data + analytics + Excel/PDF export)"
requires-python = ">=3.10"
